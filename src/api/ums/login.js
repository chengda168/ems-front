import BaseApi from '@/api/request/baseApi.js'

export default class login extends BaseApi {
    static async login(){
        // const url = `/archive/classify`;
        // const data = await this.$get(url);
        // return data;
    }
 
}